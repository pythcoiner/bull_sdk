// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../../api/simple.dart';
import '../../../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_fields_are_eq`, `assert_fields_are_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`

enum CoinSource { sp, segwit, taproot, other }

class RegtestDefaults {
  final bool isOk;
  final String error;
  final String blindbitUrl;
  final String p2PNode;
  final String electrumUrl;

  const RegtestDefaults({
    required this.isOk,
    required this.error,
    required this.blindbitUrl,
    required this.p2PNode,
    required this.electrumUrl,
  });

  @override
  int get hashCode =>
      isOk.hashCode ^
      error.hashCode ^
      blindbitUrl.hashCode ^
      p2PNode.hashCode ^
      electrumUrl.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is RegtestDefaults &&
          runtimeType == other.runtimeType &&
          isOk == other.isOk &&
          error == other.error &&
          blindbitUrl == other.blindbitUrl &&
          p2PNode == other.p2PNode &&
          electrumUrl == other.electrumUrl;
}

class SpBalanceView {
  final BigInt confirmedSat;
  final BigInt totalUnifiedSat;
  final int? lastScannedHeight;

  const SpBalanceView({
    required this.confirmedSat,
    required this.totalUnifiedSat,
    this.lastScannedHeight,
  });

  @override
  int get hashCode =>
      confirmedSat.hashCode ^
      totalUnifiedSat.hashCode ^
      lastScannedHeight.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SpBalanceView &&
          runtimeType == other.runtimeType &&
          confirmedSat == other.confirmedSat &&
          totalUnifiedSat == other.totalUnifiedSat &&
          lastScannedHeight == other.lastScannedHeight;
}

class SpCoinView {
  final String outpoint;
  final BigInt amountSat;
  final int height;
  final bool isSpendable;
  final String? label;

  const SpCoinView({
    required this.outpoint,
    required this.amountSat,
    required this.height,
    required this.isSpendable,
    this.label,
  });

  @override
  int get hashCode =>
      outpoint.hashCode ^
      amountSat.hashCode ^
      height.hashCode ^
      isSpendable.hashCode ^
      label.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SpCoinView &&
          runtimeType == other.runtimeType &&
          outpoint == other.outpoint &&
          amountSat == other.amountSat &&
          height == other.height &&
          isSpendable == other.isSpendable &&
          label == other.label;
}

enum SpNetwork { bitcoin, signet, testnet, regtest }

/// Typed payment direction surfaced to Dart so consumers can `match` on
/// it exhaustively instead of string-comparing.
enum SpPaymentDirection { receive, send, selfSend }

class SpPaymentView {
  final String txid;
  final SpPaymentDirection direction;
  final BigInt amountSat;
  final BigInt? feeSat;
  final int? height;
  final BigInt? timestamp;
  final String? label;

  const SpPaymentView({
    required this.txid,
    required this.direction,
    required this.amountSat,
    this.feeSat,
    this.height,
    this.timestamp,
    this.label,
  });

  @override
  int get hashCode =>
      txid.hashCode ^
      direction.hashCode ^
      amountSat.hashCode ^
      feeSat.hashCode ^
      height.hashCode ^
      timestamp.hashCode ^
      label.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SpPaymentView &&
          runtimeType == other.runtimeType &&
          txid == other.txid &&
          direction == other.direction &&
          amountSat == other.amountSat &&
          feeSat == other.feeSat &&
          height == other.height &&
          timestamp == other.timestamp &&
          label == other.label;
}

enum SubAccountKind { segwit, taproot }

class TxSimulation {
  final List<UnifiedCoinView> inputs;
  final List<RecipientView> outputs;
  final BigInt feeSat;
  final BigInt changeSat;

  const TxSimulation({
    required this.inputs,
    required this.outputs,
    required this.feeSat,
    required this.changeSat,
  });

  @override
  int get hashCode =>
      inputs.hashCode ^ outputs.hashCode ^ feeSat.hashCode ^ changeSat.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is TxSimulation &&
          runtimeType == other.runtimeType &&
          inputs == other.inputs &&
          outputs == other.outputs &&
          feeSat == other.feeSat &&
          changeSat == other.changeSat;
}

enum UnifiedCoinStatus { unconfirmed, unspent, spent }

class UnifiedCoinView {
  final CoinSource source;
  final String outpoint;
  final BigInt amountSat;
  final int? height;
  final UnifiedCoinStatus status;

  const UnifiedCoinView({
    required this.source,
    required this.outpoint,
    required this.amountSat,
    this.height,
    required this.status,
  });

  @override
  int get hashCode =>
      source.hashCode ^
      outpoint.hashCode ^
      amountSat.hashCode ^
      height.hashCode ^
      status.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is UnifiedCoinView &&
          runtimeType == other.runtimeType &&
          source == other.source &&
          outpoint == other.outpoint &&
          amountSat == other.amountSat &&
          height == other.height &&
          status == other.status;
}
